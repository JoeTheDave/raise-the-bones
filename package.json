{
  "name": "raise-the-bones",
  "version": "1.0.36",
  "description": "Bootstrap a full-stack React + Express + Prisma application",
  "type": "module",
  "bin": {
    "raise-the-bones": "./dist/index.js"
  },
  "files": [
    "dist",
    "template"
  ],
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "prepublishOnly": "npm run build && chmod +x dist/index.js && ls -la dist/index.js",
    "publish-cli": "npm run build && chmod +x dist/index.js && npm publish"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^12.0.0",
    "fs-extra": "^11.2.0"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^20.10.5",
    "typescript": "^5.2.2"
  }
}
